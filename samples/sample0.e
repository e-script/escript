Hello every body. Today I gonna introduce to you a new programming language.

This function calculate the sum of two number
operators = {
    align = 2
    sum = { a = params[0], b = params[1] } -> {
        result = a + b + parent.align
    }
}

This line compute the value of a
array = [1 2 3 4 + 5]
a = 1 + 2 * 3 + 4

sum2 = array

This line return result value
result = a * 5 + array[3] + operators.sum(1, 2)